<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kgu.www.service.dao.KguDAO">

	<insert id="bookInsertForm">
		INSERT INTO  book 
     	VALUES (#{book_picture}, #{book_name}, #{book_writer}, #{book_price},
     	#{book_info}, #{book_mokcha}, #{book_inventory})
	</insert>
	
	<select id = "BookAll" resultType = "BookVO">
		SELECT book_picture, book_name, book_writer, book_price, book_info, book_mokcha, book_inventory 
		FROM book
	</select>
	
	<select id="getBookInfo" resultType = "BookVO">
		SELECT book_picture, book_name, book_writer, book_price, book_info, book_mokcha, book_inventory 
		FROM book
		WHERE book_name = #{book_name}
	</select>
</mapper>