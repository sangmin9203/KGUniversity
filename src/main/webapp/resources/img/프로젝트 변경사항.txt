DB
CREATE TABLE BOOK(
book_picture varchar2(300),
book_name varchar2(100) primary key,
book_writer varchar2(50),
book_price int,
book_info clob,
book_mokcha clob,
book_inventory int
);


1) web.xml 에 강제인코딩 UTF-8 삽입
2) 일단 index.jsp 해서 book_insert.jsp 로 넘어가게만들겠음
3) book_insert 페이지 추가 일단 책 추가부터 진행해 보려고한다.
그전에 연결이 되게끔 bulid path 랑 fact?? 확인부터하자

업로드를 하기 위해
1. pom.xml 추가
<dependency>
    <groupId>javax.servlet</groupId>
    <artifactId>servlet-api</artifactId>
    <version>3.0-alpha-1</version>
    <scope>provided</scope>
</dependency>
<dependency>
	<groupId>commons-fileupload</groupId>
	<artifactId>commons-fileupload</artifactId>
	<version>1.3.2</version>
</dependency>


2. servlet-context.xml에 다음을 추가

<beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver"> 
	<beans:property name="maxUploadSize" value="20971520">
	</beans:property> 
</beans:bean>

3. 파일 경로에 한글이 있어서 log4j 가 제대로 작동을 안하기 때문에
pom.xml 에
<!--

[오류 원인] 한글때문에 xml 파싱시 오류날 경우 (URI$MalformedURIException 오류) 

[오류 내역] com.sun.org.apache.xerces.internal.util.URI$MalformedURIException: Path contains invalid character: 자

[소스 시작]

-->

<dependency>

<groupId>xerces</groupId>

<artifactId>xercesImpl</artifactId>

<version>2.9.1</version>

</dependency>

<!-- [소스 종료] -->
를 추가하였다.

4. 지금은 HomeController에서 작업을 하기 때문에 @Resource를 해줬어야 하는데 그걸 안해서 지금까지 DB에 삽입이 되지 않았던 것이었음 이거 확인 해아한다.

5. img 파일에 이미지가 저장되도록 만들었지만 경로가 내 pc를 기초로해서 각 컴퓨터마다 경로를 변경해줘야한다.

6. 책 등록은 여기서 끝인거 같음.

###. 내일 할일
파일 폴더 하나 만들어서 이미지 저장
그리고 이제 책 판매 목록 만들어야한다.